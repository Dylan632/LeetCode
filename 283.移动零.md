---
type: algorithm
tags:
  - 双指针
category: 数组
difficulty: 🟢 Easy
leetcode: 283
created: 2026-02-13 23:24
obsidian-note-status:
  - colorful:completed
---

## 题目
![[283.png]]

## 思路（必填）
1. `fast` 指针负责寻找数组中的非零元素。
2. `slow` 指针维护“已排好序的非零序列”的下一个位置。
3. 遇到非零元素就将其移动到 `slow` 位置，并自增 `slow`。
![[283.移动零.gif]]
## 关键点
> [!Tip]

## 代码

```java
class Solution {  
    public void moveZeroes(int[] nums) {  
        // 使用双指针：slow 指向下一个非零元素应该存放的位置  
        int slow = 0;  
        for (int fast = 0; fast < nums.length; fast++) {  
            if (nums[fast] != 0) {  
                // 如果快慢指针不相等，说明中间有零，进行交换  
                if (fast != slow) {  
                    nums[slow] = nums[fast];  
                    nums[fast] = 0;  
                }  
                slow++;  
            }  
        }  
    }  
}
```

## 复杂度

- Time: O()
- Space: O()

## 相似题目

- [[27.移除元素]]

