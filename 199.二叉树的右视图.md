---
type: algorithm
tags:
category: 二叉树
difficulty: 🟡 Medium
leetcode: 199
created: 2026-04-08 12:21
---

## 题目

![[199.png]]

**提示：**

- 二叉树的节点个数的范围是 `[0, 100]`
- `-100 <= Node.val <= 100`

## 思路

### 问题分析

二叉树的右视图是从右侧观察树时能看到的节点序列，即每一层的最右侧节点。需要按从上到下的顺序返回这些节点值。

### 算法选择

选择 BFS（广度优先搜索）层序遍历算法。因为需要逐层处理节点，BFS 天然适合按层遍历的场景。

### 实现步骤

1. 处理空树的边界情况，返回空列表
2. 初始化当前层节点列表，初始包含根节点
3. 循环处理每一层：
    - 获取当前层最后一个节点的值（右视图节点）
    - 遍历当前层所有节点，收集其非空子节点到下一层列表
    - 将当前层更新为下一层继续处理
4. 当没有节点时退出循环，返回结果

## 关键点

> [!Tip] 
> 使用 `getLast()` 方法获取列表最后一个元素，时间复杂度 O(1)，避免了队列两端的操作开销。

## 代码

```java
class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        if (root == null) {
            return List.of();
        }
        List<Integer> ans = new ArrayList<>();
        List<TreeNode> cur = List.of(root);
        while (!cur.isEmpty()) {
            ans.add(cur.getLast().val);
            List<TreeNode> nxt = new ArrayList<>();
            for (TreeNode node : cur) {
                if (node.left != null) {
                    nxt.add(node.left);
                }
                if (node.right != null) {
                    nxt.add(node.right);
                }
            }
            cur = nxt;
        }
        return ans;
    }
}
```

## 复杂度

- Time: O(n)，其中 n 为二叉树的节点个数。虽然写了个二重循环，但每个节点只会添加到列表中一次，所以总的循环次数是节点个数之和，即 O(n)。
- Space: O(n)，满二叉树（每一层都填满）最后一层有大约 n/2 个节点，因此节点数组中最多有 O(n) 个元素，所以空间复杂度是 O(n) 的。

## 相似题目

- [[102.二叉树的层序遍历]]
- [[103.二叉树的锯齿形层序遍历]]
