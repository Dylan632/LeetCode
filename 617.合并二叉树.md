---
type: algorithm
tags:
category: 二叉树
difficulty: 🟢 Easy
leetcode: 617
created: 2026-02-11 14:54
obsidian-note-status:
  - colorful:completed
---

## 题目
![[617.png]]

## 思路（必填）

1. 递归终止：若 root1 为空，返回 root2（反之亦然）——不重叠部分直接挂上
2. 合并当前节点：两个节点的值相加
3. 递归合并左子树：mergeTrees(root1.left, root2.left)
4. 递归合并右子树：mergeTrees(root1.right, root2.right)
5. 返回合并后的根节点
  
```
递归调用过程
  mergeTrees(1, 2)
  ├── merged = new TreeNode(1+2 = 3)
  ├── merged.left = mergeTrees(3, 1)
  │   ├── merged = new TreeNode(3+1 = 4)
  │   ├── merged.left = mergeTrees(5, null)
  │   │   └── root2==null, return root1(5)  ✅ 返回 节点5
  │   ├── merged.right = mergeTrees(null, 4)
  │   │   └── root1==null, return root2(4)  ✅ 返回 节点4
  │   └── merged = TreeNode(4, left=5, right=4)  ✅ 返回 节点4
  ├── merged.right = mergeTrees(2, 3)
  │   ├── merged = new TreeNode(2+3 = 5)
  │   ├── merged.left = mergeTrees(null, null)
  │   │   └── root1==null, return root2(null)  ✅ 返回 null
  │   ├── merged.right = mergeTrees(null, 7)
  │   │   └── root1==null, return root2(7)  ✅ 返回 节点7
  │   └── merged = TreeNode(5, left=null, right=7)  ✅ 返回 节点5
  └── merged = TreeNode(3, left=4, right=5)  ✅ 返回 节点3
```

## 关键点
>[!Tip] 当一侧为 null 时直接返回另一侧，这既是递归终止条件，也巧妙处理了两棵树形状不同的情况——不需要逐个节点复制。

## 代码

```java
class Solution {  
    public TreeNode mergeTrees(TreeNode root1, TreeNode root2) {  
        if (root1 == null) {  
            return root2;  
        }  
        if (root2 == null) {  
            return root1;  
        }  
        TreeNode merged = new TreeNode(root1.val + root2.val);  
        merged.left = mergeTrees(root1.left, root2.left);  
        merged.right = mergeTrees(root1.right, root2.right);  
        return merged;  
    }  
}
```

## 复杂度

- Time: O(min(m, n))
- Space: O(min(m, n))
 m、n 为两棵树的节点数，只遍历重叠部分；空间为递归栈深度，最坏退化为链表时等于 min(m,n)
---

## 相似题目

- [[ ]]

