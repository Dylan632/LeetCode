---
type: algorithm
tags: 递归
category: 链表
difficulty: 🟢 Easy
leetcode: 24
created: 2026-02-10 15:46
obsidian-note-status:
  - colorful:completed
---

## 题目
![[24.png]]

## 思路（必填）

1. 创建 `dummy` 节点，避免头节点特殊处理。
2. `prev` 从 `dummy` 出发，每轮找到待交换的 `first` 和 `second`。
3. 三步指针操作完成交换：`prev→second`、`first→second`的后继、`second→first`。
4. `prev` 前进到 `first`（交换后它在后面），准备处理下一对。

## 关键点

>[!TIP] 关键点：dummy 只在第一轮被间接修改了 next，之后 prev 脱离 dummy，dummy.next 始终指向交换后的新头节点。
本质：`prev` 和 `dummy` 一开始是同一个对象
`prev = dummy;`
<u>这行代码不是复制，而是让 prev 和 dummy 指向内存中同一个节点对象</u>。
关键的一行来了：
`prev.next = second;`   // 把 `prev` 的 `next` 指向 节点2
⚠️ 此时 `prev` 就是 `dummy`！它们是同一个对象！
所以这行代码等价于：
`dummy.next = second;`  // dummy 的 next 也变成了 节点2

| 概念 <br>              | 说明                                                          |
| -------------------- | ----------------------------------------------------------- |
| `prev = first`       | 让 `prev` 变量指向另一个对象，不影响 `dummy`                              |
| `prev.next = second` | 修改 `prev` 所指对象的属性，如果此时 `prev` 和 `dummy` 是同一个对象，`dummy` 也受影响 |
用生活类比：
`prev` 和 `dummy` 像两个人拿着同一条狗的牵引绳。
- `prev.next = second`：通过牵引绳把狗拴到了新位置 → 两个人看到的都是新位置的狗（修改对象属性）
- `prev = first`：一个人放开了绳子，去抓另一条狗 → 另一个人手里的绳子不受影响（变量重新赋值）
## 代码

```java
class Solution {  
    public ListNode swapPairs(ListNode head) {  
        ListNode dummy = new ListNode(0);  
        dummy.next = head;  
        ListNode prev = dummy;  
        // 循环条件：确保还有至少两个节点可以交换  
        while (prev.next != null && prev.next.next != null) {  
            ListNode first = prev.next;  
            ListNode second = prev.next.next;  
            prev.next = second; // dummy也是，dummy -> second  
            first.next = second.next;  
            second.next = first;  
            prev = first;  
        }  
        return dummy.next;  
    }  
}
```

## 复杂度

- Time: O(n)
- Space: O(1)

## 相似题目

- [[19.删除链表的倒数第N个节点]]
- [[21.合并两个有序链表]]
- [[206.反转链表]]

