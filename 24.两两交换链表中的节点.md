---
type: algorithm
tags: 递归
category: 链表
difficulty: 🟢 Easy
leetcode: 24
created: 2026-02-10 15:46
---

## 题目
![[24.png]]

## 思路（必填）

1. 建立 dummy -> head，统一头节点交换逻辑。
2. 用 prev 指向每一对待交换节点的前驱。
3. 取 first = prev.next、second = first.next。
4. 按固定顺序重连 3 条边完成交换。
5. prev 前进到 first，处理下一对。

## 关键点
三步重连顺序正确：cur.next -> second、first.next -> second.next、second.next -> first，不会丢链或成环。
循环条件一次性保证两节点可交换。

## 代码

```java
class Solution {
    public ListNode swapPairs(ListNode head) {
        ListNode dummy = new ListNode();
        dummy.next = head;
        ListNode prev = dummy;
        while (prev.next != null && prev.next.next != null) {
            ListNode first = prev.next;
            ListNode second = prev.next.next;
            prev.next = second;
            first.next = second.next;
            second.next = first;
            prev = first;
        }
        return dummy.next;
    }
}
```

## 答案代码
> [!TIP]  

```java

```

## 知识点


## 复杂度

- Time: O(n)
- Space: O(1)

---

## 相似题目

- [[ ]]

