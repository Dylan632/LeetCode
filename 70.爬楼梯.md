---
type: algorithm
tags: 动态规划
category: 动态规划
difficulty: 🟢 Easy
leetcode: 70
created: 2026-02-24 00:47
obsidian-note-status:
  - colorful:completed
---

## 题目
![[70.png]]


## 思路（必填）

1. 确定dp数组以及下标的含义。dp[i]： 爬到第i层楼梯，有dp[i]种方法
2. 确定递推公式：dp[i] = dp[i - 1] + dp[i - 2]
3. dp数组如何初始化：不考虑dp[0]如何初始化，只初始化dp[1] = 1，dp[2] = 2，然后从i = 3开始递推，这样才符合dp[i]的定义。
4. 确定遍历顺序：从前向后遍历
5. 举例推导dp数组

## 关键点
>[!Tip] 到达第 i 级台阶只能从第 i-1 级（跨1步）或第 i-2 级（跨2步）而来，因此 dp[i] = dp[i-1] + dp[i-2]，这本质上就是斐波那契数列。

## 代码

```java
class Solution {  
    public int climbStairs(int n) {  
        if (n <= 1) return n;  
        int[] dp = new int[n + 1];  
        dp[1] = 1;  
        dp[2] = 2;  
        for (int i = 3; i <= n; i++) {  
            dp[i] = dp[i - 1] + dp[i - 2];  
        }  
        return dp[n];  
    }  
}
```

## 优化后代码
```java
  class Solution {
      public int climbStairs(int n) {
          if (n <= 2) return n;
          // 只保留前两个状态，空间 O(1)
          int prev = 1, curr = 2;
          for (int i = 3; i <= n; i++) {
              int next = prev + curr;
              prev = curr;
              curr = next;
          }
          return curr;
      }
  }
```
空间优化：滚动变量替代 dp 数组
- 当前代码：int[] dp = new int[n + 1]，分配 n+1 大小的数组
- 建议修改：用 prev 和 curr 两个变量滚动更新
- 原因：状态转移 dp[i] = dp[i-1] + dp[i-2] 只依赖前两个值，无需保留全部历史，空间从 O(n) 降至 O(1)

## 复杂度

- Time: O(n)
- Space: O(n)

## 相似题目

- [[ ]]