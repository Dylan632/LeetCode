---
type: algorithm
tags:
category: 动态规划
difficulty: 🟡 Medium
leetcode: 139
created: 2026-02-28 22:04
obsidian-note-status:
  - colorful:complete
---

## 题目
![[139.png]]
![[139_1.png]]

**提示：**

- `1 <= s.length <= 300`
- `1 <= wordDict.length <= 1000`
- `1 <= wordDict[i].length <= 20`
- `s` 和 `wordDict[i]` 仅由小写英文字母组成
- `wordDict` 中的所有字符串**互不相同**

## 思路（必填）
### 算法选择

动态规划。dp[i] 表示 `s[0..i)` 是否可被拆分。对每个位置 i，枚举可能的分割点 j，检查“前缀可拆分”且“后缀在字典中”。

### 实现步骤
1. 字典转 HashSet，获得 O(1) 查找能力
2. 预计算 maxLen，限制内层遍历范围
3. dp[0] = true，空串可拆分
4. 对 i 从 1 到 n，遍历分割点 j（范围 [max(0, i-maxLen), i)）
5. 先查 dp[j]（O(1)），再查 wordSet.contains(s.substring(j, i))
6. 两者都为 true 则 dp[i] = true，break
7. 返回 dp[n]

### 图解

以 s = “leetcode”，字典 = [“leet”, “code”] 为例：

```
索引:  0  1  2  3  4  5  6  7  8
字符:     l  e  e  t  c  o  d  e
dp:   T  F  F  F  T  F  F  F  T
                  ↑           ↑
             “leet”命中    “code”命中
             dp[0]=T       dp[4]=T
```

**关键转折点：**
- i=4：j=0 时，dp[0]=T 且 “leet”∈字典 → dp[4]=T
- i=8：j=4 时，dp[4]=T 且 “code”∈字典 → dp[8]=T

## 关键点

> [!Tip]
> - maxLen 剪枝是核心优化——子串长度不可能超过最长单词，大幅缩小搜索空间
> - 短路顺序是隐藏优化——dp[j] 放前面，为 false 时跳过昂贵的字符串操作

## 代码

```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        Set<String> wordSet = new HashSet<>(wordDict);
        int n = s.length();

        // 记录字典中单词最大长度，限制内层遍历范围
        int maxLen = 0;
        for (String word : wordDict) {
            maxLen = Math.max(maxLen, word.length());
        }

        boolean[] dp = new boolean[n + 1];
        dp[0] = true;

        for (int i = 1; i <= n; i++) {
            // 遍历分割点，利用 HashSet O(1) 查找
            for (int j = Math.max(0, i - maxLen); j < i; j++) {
                if (dp[j] && wordSet.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        return dp[n];
    }
}
```

## 知识点

- 动态规划：用 `dp[i]` 表示子问题的解，通过状态转移求解
- HashSet 优化：O(1) 时间查找，避免线性扫描字典
- 剪枝优化：利用 `maxLen` 限制搜索空间，减少无效计算
- 短路求值：先判断 `dp[j]` 再创建 substring，避免不必要的字符串操作
## 复杂度

- Time: O(n × maxLen)，其中 n 为字符串长度，maxLen 为字典中最长单词长度。外层循环 n 次，内层循环最多 maxLen 次，每次 substring 和 HashSet 查找均为 O(maxLen)
- Space: O(n + m)，其中 n 为 dp 数组大小，m 为 HashSet 存储字典字符串的空间

## 相似题目

- [[70.爬楼梯]]
- [[322.零钱兑换]]
- [[279.完全平方数]]

