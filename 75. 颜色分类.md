---
type: algorithm
tags:
category:
difficulty: 🟡 Medium
leetcode: 75
created: 2026-03-30 23:49
---

## 题目

![[75.png]]

## 思路（必填）

### 问题分析

- 输入：包含 0、1、2 的数组（分别代表红、白、蓝）
- 目标：原地排序，使 0 在前，1 在中，2 在后
- 约束：仅使用常数空间，一次遍历

### 算法选择

采用三指针交换策略（荷兰国旗问题的经典解法）：

- p0：追踪 0 的插入位置
- p1：追踪 1 的插入位置
- i：当前遍历位置

### 实现步骤

1. 遍历每个元素，先将当前位置置为 2（最右侧值）
2. 如果当前值 ≤ 1，在 p1 处放置 1，并右移指针
3. 如果当前值 = 0，在 p0 处放置 0，并右移指针
4. 由于先放 1 后放 0，0 会覆盖之前的 1，保证顺序正确

## 关键点

> [!Tip]
> - 利用覆盖顺序保证相对位置：先覆盖 2，再覆盖 1，最后覆盖 0
> - p0 ≤ p1 ≤ i 始终成立，保证不会覆盖已排好的元素
> - 这种写法比传统的三向切分更简洁，但思想相同

## 代码

```java
//leetcode submit region begin(Prohibit modification and deletion)  
class Solution {  
    public void sortColors(int[] nums) {  
        int p0 = 0;  
        int p1 = 0;  
        for (int i = 0; i < nums.length; i++) {  
            int x = nums[i];  
            nums[i] = 2;  
            if (x <= 1) {  
                nums[p1++] = 1;  
            }  
            if (x == 0) {  
                nums[p0++] = 0;  
            }  
        }  
    }  
}  
//leetcode submit region end(Prohibit modification and deletion)
```

## 知识点

- 三指针技巧
- 荷兰国旗问题

## 复杂度

- Time: O(n)，只需要遍历数组一次
- Space: O(1)，只使用了几个常数级别的变量指针，没有额外开辟空间

## 相似题目

- [[283.移动零]]
- [[215.数组中的第K个最大元素]]
- [[912. 排序数组]]
