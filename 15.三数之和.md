---
type: algorithm
tags:
category: 双指针
difficulty: 🟡 Medium
leetcode: 15
created: 2026-02-14 00:34
obsidian-note-status:
  - colorful:completed
---

## 题目
![[15.png]]

## 思路（必填）
![[15.三数之和.gif]]
1. **先排序数组**，这样方便寻找和为 0 的组合，并且便于去重。
2. 用**一个循环遍历数组**，将当前元素固定为三元组的第一个数 `nums[i]`。
3. 在剩余右侧部分用 **两个指针 `left` 和 `right`**：  
    - `left = i+1`
    - `right = n−1`
4. 计算三数之和：`sum = nums[i] + nums[left] + nums[right]`：
	1. 若 `sum < 0` → 说明和太小，左指针往右移（向大数逼近）
	2. 若 `sum > 0` → 说明和太大，右指针往左移（向小数逼近）
	3. 若 `sum == 0` → 找到一组合法三元组
5. 每次找到合法三元组后需进行 **去重**，跳过重复的值以避免重复结果。

## 关键点
>[!Tip] 排序 + 双指针
### 1. 排序的意义
排序后数组有序，可以用左右指针根据当前和快速调整方向。 这样把复杂度从 **O(n³)** 降到 **O(n²)**。
### 2. 外层去重
```java
if (i > 0 && nums[i] == nums[i - 1]) {  
    continue;  
}
```
避免同样的三元组合在不同 i 下重复出现。
### 3.内层左右指针去重
```java
left++;  
// 左指针去重  
  while (left < right && nums[left] == nums[left - 1]) {  
    left++;  
}  
right--;  
// 右指针去重  
  while (left < right && nums[right] == nums[right + 1]) {  
     right--;  
 }
```

## 代码

```java
class Solution {  
    public List<List<Integer>> threeSum(int[] nums) {  
        Arrays.sort(nums); // 先排序  
        List<List<Integer>> res = new ArrayList<>();  
        // 固定i，找j和k  
        for (int i = 0; i < nums.length - 2; i++) {  
            // 提前剪枝：如果当前 nums[i] 已经大于 0，那么和一定 > 0，后面就不用查找  
            if (nums[i] > 0) {  
                break;  
            }  
            // 去重：跳过和上一个相同的 nums[i]            
            if (i > 0 && nums[i] == nums[i - 1]) {  
                continue;  
            }  
            int left = i + 1;  
            int right = nums.length - 1;  
            while (left < right) {  
                int sum = nums[i] + nums[left] + nums[right];  
                if (sum > 0) {  
                    right--; // 和大 → 右指针左移  
                } else if (sum < 0) {  
                    left++; // 和小 → 左指针右移  
                } else {  
                    res.add(Arrays.asList(nums[i], nums[left], nums[right]));  
                    left++;  
                    // 左指针去重  
                    while (left < right && nums[left] == nums[left - 1]) {  
                        left++;  
                    }  
                    right--;  
                    // 右指针去重  
                    while (left < right && nums[right] == nums[right + 1]) {  
                        right--;  
                    }  
                }  
            }  
        }  
        return res;  
    }  
}
```

## 知识点


## 复杂度

- Time: O(n²)
- Space: O(1)

## 相似题目

- [[ ]]

