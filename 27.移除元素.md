---
type: algorithm
tags: 双指针
category: 数组
difficulty: 🟢 Easy
leetcode: 27
created: 2026-02-04 10:38
obsidian-note-status:
  - colorful:completed
---

## 题目
![[27.png]]
## 思路（必填）
1. 初始化 slow = 0 作为写入位置
2. fast 遍历整个数组
3. 遇到 nums[fast] != val，将其写入 slowIndex 位置，慢指针后移
4. 遇到 nums[fast] == val，跳过（快指针继续，慢指针不动）
5. 最终 slow 就是新数组长度
## 关键点
慢指针始终指向“下一个应该存放非 val 元素的位置”

## 代码

```java
class Solution {  
    public int removeElement(int[] nums, int val) {  
        int slow = 0;  // 慢指针：站在起跑线，准备接收“好果子”
        // 快指针：从头跑到尾，检查每一个果子
        for (int fast = 0; fast < nums.length; fast++) {
        // 如果快指针发现这个果子“不是烂果子”(不是 val)  
            if (nums[fast] != val) {  
            // 就把它扔给慢指针存起来
                if (slow != fast) { // 避免自我赋值  
                // 慢指针往前挪一格，准备接下一个
                    nums[slow] = nums[fast];  
                }  
                slow++;  
            } 
            // 如果是烂果子(== val)，快指针直接跳过，慢指针原地不动 
        } 
        // 最后慢指针的位置，刚好就是新数组的长度 
        return slow;  
    }  
}
```
## 知识点


## 复杂度

- Time: O(n)
- Space: O(1)
## 相似题目

- [[42.接雨水]]

