---
type: algorithm
tags:
  - 递归
category: 二叉树
difficulty: 🟢 Easy
leetcode: 226
created: 2026-02-11 14:55
obsidian-note-status:
  - colorful:completed
---

## 题目
![[226.png]]

## 思路

1. 递归终止条件：节点为 null，返回 null
2. 递归翻转左子树 invertTree(root.left)
3. 递归翻转右子树 invertTree(root.right)
4. 交换当前节点的左右孩子指针
5. 返回当前节点

## 关键点
> [!Tip] 理解“翻转”是自底向上的——每个子树都是独立翻转的，最后在父节点处交换指针即可。后序遍历保证了子树先被完全翻转，再做交换。

## 代码

```java
class Solution {
    public TreeNode invertTree(TreeNode root) {
        if (root == null) {
            return null;
        }
        // 后序遍历：先递归翻转左右子树，再交换
        invertTree(root.left);
        invertTree(root.right);
        // 交换左右孩子
        TreeNode temp = root.left;
        root.left = root.right;
        root.right = temp;
        return root;
    }
}
```

## 知识点

`root` 的真实身份是：”当前正在处理的这个节点”。
### 🌳 递归图
以如下树为例：
```
输入:          输出:
     4             4
   /   \         /   \
  2     7   →   7     2
 / \   / \     / \   / \
1   3 6   9   9   6 3   1
```
完整递归过程：
```
invertTree(4)
│
├── invertTree(2)                          ← 递归左子树
│   ├── invertTree(1)                      ← 递归左子树
│   │   ├── invertTree(null) → null        ← 叶子，返回
│   │   ├── invertTree(null) → null        ← 叶子，返回
│   │   ├── swap(1): left ⇔ right (null ⇔ null，无变化)
│   │   └── return 1 ✅
│   │
│   ├── invertTree(3)                      ← 递归右子树
│   │   ├── invertTree(null) → null
│   │   ├── invertTree(null) → null
│   │   ├── swap(3): left ⇔ right (null ⇔ null，无变化)
│   │   └── return 3 ✅
│   │
│   ├── swap(2): left ⇔ right (1 ⇔ 3)         ← 交换！
│   │       2              2
│   │      / \    →       / \
│   │     1   3          3   1
│   └── return 2 ✅
│
├── invertTree(7)                          ← 递归右子树
│   ├── invertTree(6)
│   │   ├── invertTree(null) → null
│   │   ├── invertTree(null) → null
│   │   ├── swap(6): 无变化
│   │   └── return 6 ✅
│   │
│   ├── invertTree(9)
│   │   ├── invertTree(null) → null
│   │   ├── invertTree(null) → null
│   │   ├── swap(9): 无变化
│   │   └── return 9 ✅
│   │
│   ├── swap(7): left ⇔ right (6 ⇔ 9)         ← 交换！
│   │       7              7
│   │      / \    →       / \
│   │     6   9          9   6
│   └── return 7 ✅
│
├── swap(4): left ⇔ right (2 ⇔ 7)             ← 最后交换根节点！
│       4              4
│      / \    →       / \
│    [2]  [7]       [7]  [2]
│    /\   /\        /\    /\
│   3  1 9  6      9  6  3  1
│
└── return 4 ✅  最终结果！
```

### 逐步状态变化图

```
原始树:        [1]swap(1):    [2]swap(3):    [3]swap(2):
     4           4              4              4
   /   \       /   \          /   \          /   \
  2     7     2     7        2     7        2     7
 / \   / \   / \   / \      / \   / \      / \   / \
1   3 6   9 1   3 6   9    1   3 6   9    3   1 6   9
             (无变化)       (无变化)        ↑交换↑

[4]swap(6):    [5]swap(9):    [6]swap(7):    [7]swap(4):
     4              4            4              4
   /   \          /   \        /   \          /   \
  2     7        2     7      2     7        7     2
 / \   / \      / \   / \    / \   / \      / \   / \
3   1 6   9    3   1 6   9  3   1 9   6    9   6 3   1
      (无变化)      (无变化)        ↑交换↑      ↑交换↑
                                             最终结果!
```

### 后序遍历的核心规律

```
        invertTree(root)
           /        \          ↑
   invertTree(L)  invertTree(R)  swap(root)
     “先左”         “再右”       “最后中”
```

自底向上：叶子节点先处理（交换 null 无影响），然后逐层向上，每一层交换完毕后，父节点再做交换，最终完成整棵树的翻转。

## 复杂度

- **Time**: O(n)
- **Space**: O(h)
- n 为节点数，h 为树高；最坏 O(n)（退化链表），最好 O(log n)（完全二叉树）

## 相似题目

- [[101.对称二叉树]]
- [[104.二叉树的最大深度]]
- [[543.二叉树的直径]]
