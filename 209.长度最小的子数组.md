---
type: algorithm
tags: 二分查找、滑动窗口
category: 数组
difficulty: 🟢 Easy
leetcode: 209
created: 2026-02-05 16:17
---

## 题目
![[209.png]]

## 思路（必填）

1. 
2. 
3. 

## 关键点
本题中，主要要理解滑动窗口如何移动 窗口起始位置，达到动态更新窗口大小的，从而得出长度最小的符合条件的长度。**滑动窗口的精妙之处在于根据当前子序列和大小的情况，不断调节子序列的起始位置。从而将O(n^2)的暴力解法降为O(n)。**

## 代码

```java
class Solution {  
    public int minSubArrayLen(int target, int[] nums) {  
        int left = 0;  
        int sum = 0;  
        int result = Integer.MAX_VALUE;  
        for (int right = 0; right < nums.length; right++) {  
            sum += nums[right];  
            while (sum >= target) {  
                result = Math.min(result, right - left + 1);  
                sum -= nums[left++];  
            }  
        }  
        //没找到符合要求的子数组，便返回0  
        return result == Integer.MAX_VALUE ? 0 :result;  
    }  
}
```

## 答案代码
> [!TIP]  

```java

```

## 知识点
`条件判断 ? 结果A : 结果B`
“它是真的吗？**如果是**，取 A；**否则**，取 B。”
- **`?`** 前面是**条件**（返回 true 或 false）。
- **`:`** 前面是**条件成立**时的值。
- **`:`** 后面是**条件不成立**时的值。

## 复杂度

- Time: O()
- Space: O()

---

## 相似题目

- [[ ]]

