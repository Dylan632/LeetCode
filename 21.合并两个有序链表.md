---
type: algorithm
tags:
category: 链表
difficulty: 🟢 Easy
leetcode: 21
created: 2026-02-11 14:53
obsidian-note-status:
  - colorful:completed
---

## 题目
![[21.png]]

## 思路（必填）

1. 使用一个 `dummy` 头节点来简化代码，避免处理头节点的特殊情况。
2. 使用 `cur` 指针来跟踪合并后的链表，逐个连接节点。  
3. 如果 `list1` 或 `list2` 遍历完了，则直接连接剩余的部分。  

## 关键点
> [!Tip] 使用双指针法，遍历两个链表，逐步合并元素。 每次选择较小的元素加入新链表，直到一个链表遍历完，最后将另一个链表连接到结果链表。
>- 每次选择较小的元素并更新 `cur` 指针，保证了链表的顺序。
>- 使用 `dummy` 节点简化了边界处理，避免了为空的特殊处理。  

## 代码

```java
class Solution {  
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {  
        ListNode dummy = new ListNode();  
        ListNode cur = dummy;  
        while (list1 != null && list2 != null) {  
            if (list1.val > list2.val) {  
                // 将list2中的当前节点链接到合并后的链表中。  
                cur.next = list2;  
                // 将list2指向链表中的下一个节点，以便在下一次循环中继续比较和合并。 
                list2 = list2.next;  
            } else {  
                cur.next = list1;  
                list1 = list1.next;  
            }  
            // cur节点前移一位  
            cur = cur.next;  
        }  
        // 连接剩余部分  
        cur.next = (list1 != null) ? list1 : list2;  
        return dummy.next;  
    }  
}
```

## 复杂度

- Time: O(n)
- Space: O(1)

## 相似题目

- [[ ]]

