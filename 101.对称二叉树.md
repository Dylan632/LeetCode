---
type: algorithm
tags:
  - 递归
category: 二叉树
difficulty: 🟢 Easy
leetcode: "101"
created: 2026-02-11 15:26
obsidian-note-status:
  - colorful:completed
---

## 题目
![[101.png]]

## 思路（必填）

1. 特判根节点为空，直接返回 true
2. 调用 compare(root.left, root.right) 比较左右子树
3. 在 compare 中，先处理 null 边界，再比较节点值
4. 递归比较外侧（左左 vs 右右）和内侧（左右 vs 右左）

## 关键点
> [!Tip] 理解“对称”等价于“外侧对应 + 内侧对应”，即 compare(left.left,right.right) 和 compare(left.right, right.left)。

## 代码

```java
class Solution {  
    public boolean isSymmetric(TreeNode root) {  
        if (root == null) {  
            return true;  
        }  
        return compare(root.left, root.right);  
    }  
  
	private boolean compare(TreeNode left, TreeNode right) {  
        if (left == null && right == null) {  
            return true;  
        }  
        if (left == null || right == null) {  
            return false;  
        }  
        if (left.val != right.val) {  
            return false;  
        }  
  
        boolean compareOutside = compare(left.left, right.right);  
        boolean compareInside = compare(left.right, right.left);  
        return compareOutside && compareInside;  
    }  
}
```

## 知识点
🌳 **递归图**
以如下对称树为例：
```
        1
       / \
      2   2
     / \ / \
    3  4 4  3
```

```
compare(2, 2)
├── 值相同 ✓ (2 == 2)
│
├── 外侧: compare(2.left, 2.right) = compare(3, 3)
│   ├── 值相同 ✓ (3 == 3)
│   ├── 外侧: compare(3.left, 3.right) = compare(null, null) → true ✅
│   ├── 内侧: compare(3.right, 3.left) = compare(null, null) → true ✅
│   └── return true && true = true ✅
│
├── 内侧: compare(2.right, 2.left) = compare(4, 4)
│   ├── 值相同 ✓ (4 == 4)
│   ├── 外侧: compare(4.left, 4.right) = compare(null, null) → true ✅
│   ├── 内侧: compare(4.right, 4.left) = compare(null, null) → true ✅
│   └── return true && true = true ✅
│
└── return true && true = true ✅

最终结果: isSymmetric = true ✅
```

## 复杂度

- **Time:** O(n) — n 为节点数，每个节点访问一次
- **Space:** O(h) — h 为树高；最坏 O(n)（退化链表），最好 O(log n)（完全二叉树）

## 相似题目

- [[226.翻转二叉树]]
- [[104.二叉树的最大深度]]

